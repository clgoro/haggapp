<% unless  current_user %>
	<div class="container-fluid">
		<div class="row text-center">
			<h3>Sign up</h3>
		 	<%= render 'form' %>
		</div>
	</div>
<% else %>
	<%= link_to "New post", new_post_path %>
<% end %>
